\set QUIET on
begin; set statement_timeout to 30000; commit;
\x auto
-- \timing on -- measures locally, includes network request
\pset null ' ∅'
\set VERBOSITY verbose
\set HISTCONTROL ignoredups
--\set HISTFILE ~/.psql/history- :DBNAME
\set PROMPT1 '>>> ':PROMPT1
\set PROMPT2 '>>> ':PROMPT2
-- \set PROMPT3 '>>> ':PROMPT3
\setenv LESS -X
\set QUIET off
