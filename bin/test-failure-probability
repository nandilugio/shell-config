#!/bin/bash

test_command=$1
test_command_md5=$(echo $test_command | md5sum)
test_command_short_md5=${test_command_md5:0:6}

now=$(date +%y%m%d_%H%M%S)

touch test_exit_codes.log test_failures.log current_test_output.log
truncate -s0 test_exit_codes.log test_failures.log current_test_output.log

echo -e "\nMD5 for experiment (test command): $test_command_md5 ($test_command_short_md5)"
echo -e "Tail -f test_exit_codes.log and/or test_failures.log to more closelly follow tests.\n"

test_number=1
while true; do
  echo -e "\n#################### Test run number $test_number for $test_command_short_md5 ####################\n"

  $test_command 2>&1 | tee current_test_output.log
  exit_code=${PIPESTATUS[0]}
  
  echo $exit_code >>test_exit_codes.log
  
  if [[ "$exit_code" != 0 ]]; then
    cat current_test_output.log >>test_failures.log
  fi
  rm current_test_output.log

  echo -e "\nTest $test_number for $test_command_short_md5 finalized with exit code $exit_code\n"

  trap '' 2
  read -t2 -N1 -p"To stop test iteration type 'q'..." response
  read_exit_code=$?
  trap 2
  if [[ "$read_exit_code" == 0 && "$response" == q ]]; then
    break
  fi
  echo
  
  test_number=$(( test_number + 1 ))
done

total_test_count=$(cat test_exit_codes.log | wc -l)
failed_test_count=$(grep -v 0 test_exit_codes.log | wc -l)
failure_frequency_pct=$(( failed_test_count * 100 / total_test_count ))%

echo -e "\nSummary for experiment $test_command_short_md5:\n" | tee -a test_exit_codes.log
echo -e "Tests performed:   $total_test_count" | tee -a test_exit_codes.log
echo -e "Failed:            $failed_test_count" | tee -a test_exit_codes.log
echo -e "Failure frequency: $failure_frequency_pct" | tee -a test_exit_codes.log

out_file_prefix=test_at_${now}_for_${test_command_short_md5}
exit_codes_filename=${out_file_prefix}_exit_codes.txt
failures_log_filename=${out_file_prefix}_failures.log

echo -e "\nMoving results to $exit_codes_filename and $failures_log_filename."
cp test_exit_codes.log $exit_codes_filename
cp test_failures.log $failures_log_filename
rm -f test_exit_codes.log test_failures.log current_test_output.log

